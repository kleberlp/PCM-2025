@model IEnumerable<PCM.WEB.MODELS.PMOCCronograma>

<!-- Main Container -->
<main id="main-container">

    <!-- Breadcrumb -->
    <div class="bg-body-light border-b">
        <div class="content py-0 text-center">
            <nav class="breadcrumb bg-body-light mb-0">
                <a class="breadcrumb-item" href="@Url.Action("Index", "Home")">@PCM.WEB.Properties.Resources.sistema</a>
                <span class="breadcrumb-item active">@PCM.WEB.Properties.Resources.cronograma</span>
            </nav>
        </div>
    </div>
    <!-- END Breadcrumb -->

    <!-- Page Content -->
    <div class="content">        

        <!-- Listagem -->
        <div class="block block-rounded border">
            
            @using (Html.BeginForm("CronogramaPMOC", "PMOC", new { ReturnUrl = ViewBag.ReturnUrl }, FormMethod.Post, new { @class = "js-validation-bootstrap", role = "form", enctype = "multipart/form-data"  }))        
            {
                @Html.AntiForgeryToken()

                    <!-- Collapsible Tasks Navigation -->
                    <div class="js-tasks-nav d-none d-md-block">
                        <!-- Tasks Info -->
                        <div class="block block-rounded border">
                            <div class="block-header block-header-default bg-gd-default">
                                <h3 class="block-title text-white font-w600">@PCM.WEB.Properties.Resources.filtro</h3>                           
                            </div>
                            <div class="block-content">
                                <ul class="list-group push">
                                    <li class="list-group-item">
                                        <div class="row">
                                            <div class="col-md-6">@Html.DropDownList("unidade", null, PCM.WEB.Properties.Resources.unidade, new { @class = "js-select2 form-control" })</div>
                                            <div class="col-md-4">
                                                <div class="col-md-4"><input type="text" class="js-datepicker form-control" id="data" name="data" data-week-start="1" data-autoclose="true" data-today-highlight="true" data-date-format="mm/yyyy" placeholder="" value="@ViewBag.data"></div>
                                            </div>                            
                                        </div>
                                    </li>
                                    <li class="list-group-item text-right">                                                    
                                        <button type="submit" class="btn btn-square btn-secondary min-width-100 h-75" name="filtrar" id="filtrar" ><i class="si si-magnifier mr-5"></i>@PCM.WEB.Properties.Resources.filtrar</button>
                                    </li>
                                </ul>
                            </div>
                        </div>
                        <!-- END Tasks Info -->
                    </div>
                    <!-- END Collapsible Tasks Navigation -->
                          
                <div class="col-md-12 col-xl-12">
                    <!-- Toggle Tasks Navigation on mobile -->
                    <button type="button" class="btn btn-block btn-square btn-primary d-md-none mb-10" data-toggle="class-toggle" data-target=".js-tasks-nav" data-class="d-none d-md-block">@PCM.WEB.Properties.Resources.filtros</button>

                    <div class="block-content block-content-full">

                        <table class="table table-vcenter table-sm table-bordered table-hover js-dataTable-list">
                            <thead>
                                <tr class="table-secondary">
                                    @if (Session["unidade"] == "")
                                    {
                                        <th class="d-none d-md-table-cell">@PCM.WEB.Properties.Resources.unidade</th>
                                    }
                                    <th class="text-center">@PCM.WEB.Properties.Resources.tag</th>
                                    <th>@PCM.WEB.Properties.Resources.descricao</th>
                                    <th class="text-center">@ViewBag.mes_1</th>
                                    <th class="text-center">@ViewBag.mes_2</th>
                                    <th class="text-center">@ViewBag.mes_3</th>
                                    <th class="text-center">@ViewBag.mes_4</th>
                                    <th class="text-center">@ViewBag.mes_5</th>
                                    <th class="text-center">@ViewBag.mes_6</th>
                                    <th class="text-center">@ViewBag.mes_7</th>
                                    <th class="text-center">@ViewBag.mes_8</th>
                                    <th class="text-center">@ViewBag.mes_9</th>
                                    <th class="text-center">@ViewBag.mes_10</th>
                                    <th class="text-center">@ViewBag.mes_11</th>
                                    <th class="text-center">@ViewBag.mes_12</th>
                                </tr>
                            </thead>

                            <tbody>                       
                                @foreach (var item in Model)
                                {
                                <tr>
                                    @if (Session["unidade"] == "")
                                    {
                                        <td class="d-none d-md-table-cell">@Html.DisplayFor(modelItem => item.unidade)</td>
                                    }
                                    <td class="text-center">@Html.DisplayFor(modelItem => item.tag)</td>
                                    <td>@Html.DisplayFor(modelItem => item.descricao)</td>
                                    <td class="text-center badge-@item.mes_1" style="width:75px">@Html.DisplayFor(modelItem => item.mes_1)</td>
                                    <td class="text-center badge-@item.mes_2" style="width:75px">@Html.DisplayFor(modelItem => item.mes_2)</td>
                                    <td class="text-center badge-@item.mes_3" style="width:75px">@Html.DisplayFor(modelItem => item.mes_3)</td>
                                    <td class="text-center badge-@item.mes_4" style="width:75px">@Html.DisplayFor(modelItem => item.mes_4)</td>
                                    <td class="text-center badge-@item.mes_5" style="width:75px">@Html.DisplayFor(modelItem => item.mes_5)</td>
                                    <td class="text-center badge-@item.mes_6" style="width:75px">@Html.DisplayFor(modelItem => item.mes_6)</td>
                                    <td class="text-center badge-@item.mes_7" style="width:75px">@Html.DisplayFor(modelItem => item.mes_7)</td>
                                    <td class="text-center badge-@item.mes_8" style="width:75px">@Html.DisplayFor(modelItem => item.mes_8)</td>
                                    <td class="text-center badge-@item.mes_9" style="width:75px">@Html.DisplayFor(modelItem => item.mes_9)</td>
                                    <td class="text-center badge-@item.mes_10" style="width:75px">@Html.DisplayFor(modelItem => item.mes_10)</td>
                                    <td class="text-center badge-@item.mes_11" style="width:75px">@Html.DisplayFor(modelItem => item.mes_11)</td>
                                    <td class="text-center badge-@item.mes_12" style="width:75px">@Html.DisplayFor(modelItem => item.mes_12)</td>
                                </tr>
                                }
                            </tbody>
                        </table>
                    </div>

                </div>
            }        
    </div>        
    </div>           
    <!-- END Page Content -->

</main>
<!-- END Main Container -->

@section Styles {
    @Styles.Render("~/Content/datepicker")
    @Styles.Render("~/Content/dataTables")
    @Styles.Render("~/Content/select")
}

@section Scripts {
    @Scripts.Render("~/plugins/dataTables")
    @Scripts.Render("~/plugins/validation")
    @Scripts.Render("~/plugins/maxlenght")
    @Scripts.Render("~/plugins/datepicker")
    @Scripts.Render("~/plugins/select")

    <script type="text/javascript">

        var BeTableDatatables = function () {
            // Override a few DataTable defaults, for more examples you can check out https://www.datatables.net/
            var exDataTable = function () {
                jQuery.extend(jQuery.fn.dataTable.ext.classes, {
                    sWrapper: "dataTables_wrapper dt-bootstrap4"
                });
            };

            // Init simple DataTable, for more examples you can check out https://www.datatables.net/
            var initDataTableSimple = function () {
                jQuery('.js-dataTable-list').dataTable({
                    fixedHeader: false,
                    searching: false,
                    paging: false,
                    ordering: false,
                    lengthChange: false,
                    pageLength: 15,                    
                    autoWidth: true,
                    dom: 'Bfrtip',
                    buttons: [
                        { extend: 'copy', text: 'Copiar'},
                        { extend: 'print',
                       
                            text: 'Imprimir'},
                        { extend: 'excel'},
                        { extend: 'pdf', orientation: 'landscape'},
                    ],
                    language: {
                        "search": "Procurar:",
                        "lengthMenu": "Exibir _MENU_ ",
                        "zeroRecords": "Nenhum Registro Encontrado",
                        "info": "_MAX_ registro(s)",
                        "infoEmpty": "Nenhum Registro encontrado",
                        "infoFiltered": "(filtrado de _MAX_ registro(s))"
                    }
                });
            };

            return {
                init: function () {
                    // Override a few DataTable defaults
                    exDataTable();

                    // Init Datatables
                    initDataTableSimple();
                }
            };
        }();


        // Initialize when page loads
        jQuery(function () { 
            $.fn.dataTable.moment('DD/MM/YYYY HH:mm'); BeTableDatatables.init(); 
            Codebase.helpers(['datepicker', 'maxlength', 'select2']);
            $('#data').datepicker({
                format: "mm/yyyy",
                startView: "months",
                minViewMode: "months",
                language: 'pt-BR'
            })
        });

    </script>
}
