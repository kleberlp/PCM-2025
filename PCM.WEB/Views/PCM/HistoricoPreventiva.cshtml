@model IEnumerable<PCM.WEB.MODELS.PCMProgramadaHistorico>

<!-- Main Container -->
<main id="main-container">

    <!-- Breadcrumb -->
    <div class="bg-body-light border-b">
        <div class="content py-0 text-center">
            <nav class="breadcrumb bg-body-light mb-0">
                <a class="breadcrumb-item" href="@Url.Action("Index", "Home")">@PCM.WEB.Properties.Resources.sistema</a>
                <span class="breadcrumb-item active">@PCM.WEB.Properties.Resources.historico_preventiva</span>
            </nav>
        </div>
    </div>
    <!-- END Breadcrumb -->

    <!-- Page Content -->
    <div class="content">        

        <!-- Listagem -->
        <div class="block block-rounded border">
            
            @using (Html.BeginForm("HistoricoPreventiva", "PCM", new { ReturnUrl = ViewBag.ReturnUrl }, FormMethod.Post, new { @class = "js-validation-bootstrap", role = "form", enctype = "multipart/form-data"  }))        
            {
                @Html.AntiForgeryToken()

                    <input type="hidden" id="formulario" name="formulario" value="@ViewBag.formulario" />

                    <!-- Collapsible Tasks Navigation -->
                    <div class="js-tasks-nav d-none d-md-block">
                        <!-- Tasks Info -->
                        <div class="block block-rounded border">
                            <div class="block-header block-header-default bg-gd-default">
                                <h3 class="block-title text-white font-w600">@PCM.WEB.Properties.Resources.filtro</h3>                           
                            </div>
                            <div class="block-content">
                                <ul class="list-group push">
                                    <li class="list-group-item">
                                        <div class="row">
                                            <div class="col-md-3">@Html.DropDownList("unidade", null, PCM.WEB.Properties.Resources.unidade, new { @class = "js-select2 form-control" })</div>
                                            <div class="col-md-4">@Html.DropDownList("programada", null, PCM.WEB.Properties.Resources.preventiva, new { @class = "js-select2 form-control" })</div>
                                            <div class="col-md-2">@Html.DropDownList("modulo", null, PCM.WEB.Properties.Resources.modulo, new { @class = "js-select2 form-control" })</div>
                                            <div class="col-md-3">
                                                <div class="input-daterange input-group" data-date-format="dd/mm/yyyy" data-week-start="1" data-autoclose="true" data-today-highlight="true">
                                                    <input type="text" class="form-control" id="data_inicio" name="data_inicio" placeholder="De" data-week-start="1" data-autoclose="true" data-today-highlight="true" value="@ViewBag.data_inicio">
                                                    <div class="input-group-prepend input-group-append">
                                                        <span class="input-group-text font-w600">-</span>
                                                    </div>
                                                    <input type="text" class="form-control" id="data_termino" name="data_termino" placeholder="Até" data-week-start="1" data-autoclose="true" data-today-highlight="true" value="@ViewBag.data_termino">
                                                </div>
                                            </div>
                                        </div>
                                    </li>
                                    <li class="list-group-item text-right">                                                    
                                        <button type="submit" class="btn btn-square btn-secondary min-width-100 h-75" name="filtrar" id="filtrar" ><i class="si si-magnifier mr-5"></i>@PCM.WEB.Properties.Resources.filtrar</button>
                                    </li>
                                </ul>
                            </div>
                        </div>
                        <!-- END Tasks Info -->
                    </div>
                    <!-- END Collapsible Tasks Navigation -->
                          
                <div class="col-md-12 col-xl-12">
                    <!-- Toggle Tasks Navigation on mobile -->
                    <button type="button" class="btn btn-block btn-square btn-primary d-md-none mb-10" data-toggle="class-toggle" data-target=".js-tasks-nav" data-class="d-none d-md-block">@PCM.WEB.Properties.Resources.filtros</button>

                    <div class="block-content block-content-full">

                        <table class="table table-vcenter table-sm table-bordered table-hover js-dataTable-list">
                            <thead>
                                <tr class="table-secondary">
                                    @if(Session["unidade"] == "")
                                    {
                                        <th class="d-none d-md-table-cell">@PCM.WEB.Properties.Resources.unidade</th>
                                    }
                                    <th class="text-center">@PCM.WEB.Properties.Resources.data</th>
                                    <th>@PCM.WEB.Properties.Resources.descricao</th>
                                    <th class="d-none d-md-table-cell text-center">@PCM.WEB.Properties.Resources.tipo_servico</th>
                                    <th class="d-none d-md-table-cell text-center">@PCM.WEB.Properties.Resources.tipo_ordem_servico</th>
                                    <th class="text-center" style="width: 120px;">@PCM.WEB.Properties.Resources.quantidade_equipamento</th>
                                    <th class="text-center" style="width: 120px;">@PCM.WEB.Properties.Resources.valor</th>
                                    @if(ViewBag.editar)
                                    {
                                        <th data-sortable="false" class="text-center" style="width: 80px;"></th>
                                    }
                                </tr>
                            </thead>

                            <tbody>                       
                                @foreach (var item in Model)
                                {
                                    <tr>
                                        @if(Session["unidade"] == "")
                                        {
                                            <td class="d-none d-md-table-cell">@Html.DisplayFor(modelItem => item.unidade)</td>
                                        }         
                                        <td class="text-center">@Html.DisplayFor(modelItem => item.data)</td>
                                        <td>@Html.DisplayFor(modelItem => item.descricao)</td>
                                        <td class="d-none d-md-table-cell text-center">@Html.DisplayFor(modelItem => item.tipo_servico)</td>
                                        <td class="d-none d-md-table-cell text-center">@Html.DisplayFor(modelItem => item.tipo_ordem_servico)</td>
                                        <td class="text-center" style="width: 120px;">@Html.DisplayFor(modelItem => item.quantidade_equipamento)</td>
                                        <td class="text-center" style="width: 120px;">@Html.DisplayFor(modelItem => item.valor)</td>
                                        @if(ViewBag.editar || ViewBag.imprimir)
                                        {
                                            <td class="text-center" style="width: 80px;" >
                                                <div class="btn-group">
                                                    @if (ViewBag.editar)
                                                    {<a href="@Url.Action("OrdemServicoProgramada", "PCM", new {codigo_unidade = item.codigo_unidade, codigo_pcm_programada_ordem_servico = item.codigo, formulario = item.formulario, view = ViewBag.view })" class="btn btn-sm btn-secondary"><i class="fa fa-pencil"></i></a>}
                                                    @if (ViewBag.editar)
                                                    {<a href="@Url.Action("PrintPreventiva", "PCM", new {unidade = item.codigo_unidade, codigo = item.codigo })" target="_blank" class="btn btn-sm btn-secondary"><i class="si si-printer"></i></a>}
                                                    @if (ViewBag.imprimir)
                                                    {<a href="@Url.Action("PrintReportProgramada", "PCM", new { unidade = item.codigo_unidade, codigo_pcm_programada_ordem_servico = item.codigo })" class="btn btn-sm btn-secondary" target="_blank"><i class="fa fa-print"></i></a>
                                                    }
                                                    </div>
                                            </td>
                                        }                                                                                       
                                    </tr>
                                }
                            </tbody>
                        </table>
                    </div>

                </div>
            }        
    </div>        
    </div>           
    <!-- END Page Content -->

</main>
<!-- END Main Container -->

@section Styles {
    @Styles.Render("~/Content/datepicker")
    @Styles.Render("~/Content/dataTables")
    @Styles.Render("~/Content/select")
}

@section Scripts {
    @Scripts.Render("~/plugins/dataTables")
    @Scripts.Render("~/plugins/validation")
    @Scripts.Render("~/plugins/maxlenght")
    @Scripts.Render("~/plugins/datepicker")
    @Scripts.Render("~/plugins/select")

<script type="text/javascript">

    $(function () {

        var unidade = $("#unidade");
        var programada = $("#programada");

        if (unidade.val() == "" || unidade.val() == "-1") {
            programada.prop("disabled", true);
            programada.select2({placeholder: '@Html.Raw(PCM.WEB.Properties.Resources.preventiva)', allowClear: true});
        }

        unidade.change(function () {

            if (unidade.val() == "") {

                programada.empty();
                programada.prop( "disabled", true );
                programada.select2({ placeholder: '@Html.Raw(PCM.WEB.Properties.Resources.preventiva)', allowClear: true });

            } else {

                var request = { "unidade": unidade.val() };

                $.post("LoadPreventiva", request, function (obj) {
                    programada.empty();
                    option = $("<option>", { "value": "" }).text("");
                    programada.append(option);
                    $.each(obj, function (indice, result) {
                        option = $("<option>", { "value": result.codigo }).text(result.descricao);
                        programada.append(option);
                    });
                }, "json");

                programada.prop("disabled", false);
                programada.select2({placeholder: '@Html.Raw(PCM.WEB.Properties.Resources.preventiva)', allowClear: true});

            }

        });

    });

    var BeTableDatatables = function () {
        // Override a few DataTable defaults, for more examples you can check out https://www.datatables.net/
        var exDataTable = function () {
            jQuery.extend(jQuery.fn.dataTable.ext.classes, {
                sWrapper: "dataTables_wrapper dt-bootstrap4"
            });
        };

        // Init simple DataTable, for more examples you can check out https://www.datatables.net/
        var initDataTableSimple = function () {
            jQuery('.js-dataTable-list').dataTable({
                fixedHeader: false,
                searching: false,
                lengthChange: false,
                pageLength: 15,
                autoWidth: true,
                dom: 'Bfrtip',
                buttons: [
                    { extend: 'copy', text: 'Copiar'},
                    { extend: 'print',

                        text: 'Imprimir'},
                    { extend: 'excel'},
                    { extend: 'pdf', orientation: 'landscape'},
                ],
                language: {
                    "search": "Procurar:",
                    "lengthMenu": "Exibir _MENU_ ",
                    "zeroRecords": "Nenhum Registro Encontrado",
                    "info": "_MAX_ registro(s)",
                    "infoEmpty": "Nenhum Registro encontrado",
                    "infoFiltered": "(filtrado de _MAX_ registro(s))"
                }
            });
        };

        return {
            init: function () {
                // Override a few DataTable defaults
                exDataTable();

                // Init Datatables
                initDataTableSimple();
            }
        };
    }();

    // Initialize when page loads
    jQuery(function () {
        $.fn.dataTable.moment('DD/MM/YYYY'); BeTableDatatables.init();
        Codebase.helpers(['datepicker', 'maxlength', 'select2']);
    });

</script>
}
