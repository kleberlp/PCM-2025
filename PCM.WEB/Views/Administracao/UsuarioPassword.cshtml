@model PCM.WEB.MODELS.Usuario

<!-- Main Container -->
<main id="main-container">
   
    <!-- Breadcrumb -->
    <div class="bg-body-light border-b">
        <div class="content py-0 text-md-center">
            <nav class="breadcrumb bg-body-light mb-0">
                <a class="breadcrumb-item font-w600" href="@Url.Action(Session["dashboard"].ToString(), "Home")">@PCM.WEB.Properties.Resources.sistema</a>
                <a class="breadcrumb-item" href="@Url.Action("UsuarioIndex", "Administracao")">@PCM.WEB.Properties.Resources.usuario</a>
                <span class="breadcrumb-item active">@PCM.WEB.Properties.Resources.resetar_senha</span>
            </nav>
        </div>
    </div>
    <!-- END Breadcrumb -->

    <!-- Page Content -->
    <div class="content">

        <!-- Update Product -->
        <div class="row gutters-tiny">
            <!-- Basic Info -->
            <div class="col-md-12">           
                @using (Html.BeginForm("UsuarioPassword", "Administracao", new { ReturnUrl = ViewBag.ReturnUrl }, FormMethod.Post, new { @class = "js-validation-bootstrap", role = "form" }))
                {
                    @Html.AntiForgeryToken()
                    @Html.HiddenFor(model => model.codigo)

                    <div class="block block-rounded border">
                        <div class="block-header block-header-default bg-gd-default">
                            <h3 class="block-title text-white font-w600">@PCM.WEB.Properties.Resources.resetar_senha</h3>
                            <div class="block-options">
                                <button type="submit" class="btn btn-square btn-secondary min-width-100 h-75">
                                    <i class="fa fa-save mr-5"></i>@PCM.WEB.Properties.Resources.salvar
                                </button>
                                <a href="@Url.Action("UsuarioIndex", "Administracao")" class="btn btn-square btn-secondary min-width-100 h-75">
                                    <i class="si si-action-undo mr-5"></i>@PCM.WEB.Properties.Resources.voltar
                                </a>
                            </div>
                        </div>
                        <div class="block-content block-content-full"> 
                            @if (Model.unidade != "") {                       
                                <div class="form-group row">
                                    <label class="col-12">@PCM.WEB.Properties.Resources.unidade: <span class="font-w400">@Html.DisplayFor(model => model.unidade)</span></label> 
                                </div>    
                            }
                            <div class="form-group row">
                                <label class="col-12">@PCM.WEB.Properties.Resources.email: <span class="font-w400">@Html.DisplayFor(model => model.email)</span></label> 
                            </div>         
                            <div class="form-group row">
                                <label class="col-12">@PCM.WEB.Properties.Resources.nome: <span class="font-w400">@Html.DisplayFor(model => model.nome)</span></label> 
                            </div>                      
                            <div class="form-group row">
                                <label class="col-12">@PCM.WEB.Properties.Resources.apelido: <span class="font-w400">@Html.DisplayFor(model => model.apelido)</span></label> 
                            </div>                                         
                            <div class="form-group row">
                                <div class="col-md-4">
                                    <label>@PCM.WEB.Properties.Resources.senha</label>
                                    <div><input type="password" class="form-control" id="senha" name="senha" value="" maxlength="20"></div>
                                </div>                  
                                <div class="col-md-4">              
                                    <label>@PCM.WEB.Properties.Resources.confirmar_senha</label>
                                    <div ><input type="password" class="form-control" id="confirmar_senha" name="confirmar_senha" value="" maxlength="20"></div>
                                </div>                                              
                            </div>                            
                        </div>                        
                    </div>
                }
            </div>
            <!-- END Basic Info -->
        </div>
        <!-- END Update Product -->
    </div>
    <!-- END Page Content -->
</main>
<!-- END Main Container -->


@section Styles {
    @Styles.Render("~/Content/select")
}

@section Scripts {
    @Scripts.Render("~/plugins/validation")
    @Scripts.Render("~/plugins/maskedinput")
    @Scripts.Render("~/plugins/maxlenght")
    @Scripts.Render("~/plugins/select")
    
    <script>

        //AJAX
        $.ajaxSetup({ async: false });


        var FormValidation = function () {
            // Init Bootstrap Forms Validation, for more examples you can check out https://github.com/jzaefferer/jquery-validation
            var initValidationBootstrap = function () {
                jQuery('.js-validation-bootstrap').validate({
                    ignore: [],
                    errorClass: 'invalid-feedback animated fadeInDown',
                    errorElement: 'div',
                    errorPlacement: function (error, e) {
                        jQuery(e).parents('.form-group > div').append(error);
                    },
                    highlight: function (e) {
                        jQuery(e).closest('.form-group > div').removeClass('is-invalid').addClass('is-invalid');
                    },
                    success: function (e) {
                        jQuery(e).closest('.form-group > div').removeClass('is-invalid');
                        jQuery(e).remove();
                    },
                    rules: {
                        'senha': {
                            required: true
                        },
                        'confirmar_senha': {
                            required: true,
                            minlength: 5,
                            equalTo: '#senha'
                        }
                    },
                    messages: {
                        'senha': {
                            required: '@PCM.WEB.Properties.Resources.campo_requerido',
                            minlength: '@PCM.WEB.Properties.Resources.valida_senha_minimo_5'
                        },
                        'confirmar_senha': {
                            required: '@PCM.WEB.Properties.Resources.campo_requerido',
                            minlength: '@PCM.WEB.Properties.Resources.valida_confirmar_senha_minimo_5',
                            equalTo: '@PCM.WEB.Properties.Resources.erro_confirmacao_senha',
                        }
                    }
                });
            };

            return {
                init: function () {
                    // Init Bootstrap Forms Validation
                    initValidationBootstrap();
                }
            };
        }();

        // Initialize when page loads
        jQuery(function () {
            FormValidation.init();
            Codebase.helpers(['maxlength', 'select2']);
        });

    </script>
}
